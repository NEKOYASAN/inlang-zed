{
  "name": "inlang-zed",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "cargo check && node --check server/bin/server.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
